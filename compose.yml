services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: htmlshare
      POSTGRES_USER: htmlshare
      POSTGRES_PASSWORD: htmlshare
    ports:
      - "7332:5432"
    volumes:
      - htmlshare-postgres:/var/lib/postgresql/data

volumes:
  htmlshare-postgres:
